%% ID: vectors
%% QUESTIONS: firing_the_rockets, geostationary_orbit, starting_w_basics, roundabout, fairground_ride, particles_on_string, vector_vs_scalar, head_on_collision, on_ice, walking_up_hill, moment_of_rest, ballistic_pendulum, energy_in_a_wire, hailstorm, launching_a_rocket, powering_up, spouting_can, lifting_rod, the_hosepipe, three_particles, water_wheel
%% CONCEPTS: resolving_vectors, momentum, newtonii, netwoniii, momentumii, angular_circular
%% LEVEL: 2
%% TOPIC: mechanics/dynamics
%% TYPE: maths
%% TITLE: Vectors -- 1
%% ORDER: 15

\input{required.tex}
% to mark as a draft.  Comment both these lines out when complete.  The page number will return to the footer.
%\pagestyle{myheadings}
%\markright{\textcolor{red}{\textbf{DRAFT: \today}}}

\begin{document}
\addtolength{\topmargin}{-0.7 cm}
\setlength{\columnsep}{22pt}
\Concepttitle{Vectors --- 1}

\subsection*{Examples of vectors}
Vectors have a magnitude (length when drawn) and direction.  Physical examples include:\nl
$\vtr{v}$ velocity \nl
$\vtr{r}$ displacement \nl
$\vtr{E}$ electric field \nl
$\vtr{A}$ area \nl
$\vtr{f}$ force \nl

\noindent Quantities with magnitude but not direction are \emph{scalars}.  For example: \nl
$m$ mass \nl
$U$ energy \nl
$T$ temperature \nl
$q$ charge \nl

%\section{Properties of vectors}
\subsection*{Direction of vectors}
Vectors are not generally tied to an origin, see figure~\ref{fig:basic-vectors}(a) with several equivalent vectors $\vtr{v}$. A vector $\vtr{r}$ is shown from the origin to $(x,y)$.
\begin{figure}[h!]
\centering
\includegraphics[width=.9\linewidth]{../../figures/basic-vectors.eps}
\caption{(a) Several equivalent. vectors $\vtr{v}$.  A specific vector $\vtr{r}$ connects the origin to a point $\vtr{r} = (x,y)$.  (b) Tip to tail addition of two vectors (c) Subtraction of a vector -- tip to tail addition of $-\vtr{v}$ to $\vtr{u}$.  (d) Many different vectors $\vtr{v}$ with the same length (modulus) $v$. }\label{fig:basic-vectors}
\end{figure}
\subsection*{Addition and subtraction}
Traversing one vector from tail to tip and then the next gives a resultant:\nl 
$\vtr{u} + \vtr{v} \equiv \vtr{v} + \vtr{u}$, \nl
figure~\ref{fig:basic-vectors}(b). \nll 
Subtraction is the addition of $-\vtr{v}$ to $\vtr{u}$ instead, giving $\vtr{u} - \vtr{v}$, figure~\ref{fig:basic-vectors}(c).  Thus $\vtr{u}$, $\vtr{v}$ and $\vtr{u} \pm \vtr{v}$ form pairs of identical vector triangles in each of the parallelograms above.

\subsection*{Modulus or length of a vector}
The length, modulus, or magnitude of a vector $\vtr{v}$ is denoted by $v$ (without the bold, underlining or whatever denotes a vector) or sometimes $|\vtr{v}|$.  The radius\vari{v} of the circle in figure~\ref{fig:basic-vectors}(d) is the magnitude of the vectors differing only in direction.

\subsection*{Specifying vectors by their components}
\qq{Draw, from the origin, in the $x,y$ plane the two vectors $\vtr{v}_1 = (1,4)$ and $\vtr{v}_2 = (4,1)$.
\begin{enumerate}
\item Show that the vector connecting $(4,1)$ to $(1,4)$ is $\vtr{v}_1 - \vtr{v}_2$. 
\item Draw an appropriate, simple right angled triangle that would allow you to evaluate the length of $\vtr{v}_1 - \vtr{v}_2$.  What is$|\vtr{v}_1 - \vtr{v}_2| $? }{$3\sqrt{2}$ 
\end{enumerate}}

\end{document}

